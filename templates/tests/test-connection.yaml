apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-argo.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-argo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-argo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
